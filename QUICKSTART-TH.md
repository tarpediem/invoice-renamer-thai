# 🚀 คู่มือเริ่มต้นใช้งานด่วน - Invoice Renamer

คู่มือนี้จะช่วยให้คุณเริ่มใช้งานแอปพลิเคชันได้ภายในไม่กี่นาที

## 📋 สิ่งที่ต้องเตรียม

- **Node.js** 18+ ([ดาวน์โหลด](https://nodejs.org/))
- **OpenRouter API Key** (แนะนำ) หรือ **LM Studio** สำหรับใช้งานแบบออฟไลน์

## ⚡ เริ่มต้นด้วย 1 คำสั่ง

```bash
./start.sh
```

เท่านี้ก็เสร็จ! สคริปต์จะทำให้อัตโนมัติ:
- ✓ ตรวจสอบ Node.js
- ✓ ติดตั้งไลบรารี
- ✓ คอมไพล์โปรเจค
- ✓ เปิดเว็บเซิร์ฟเวอร์

## 🌐 เข้าถึงหน้าเว็บ

เมื่อเริ่มแล้ว เปิดเบราว์เซอร์:

- **หน้าหลัก**: http://localhost:3000
- **ตั้งค่า**: http://localhost:3000/settings.html

## 🔑 การตั้งค่า API Key

### วิธีที่ 1: ตัวแปรสภาพแวดล้อม (รวดเร็ว)

```bash
export OPENROUTER_API_KEY='api-key-ของคุณ'
./start.sh
```

### วิธีที่ 2: ไฟล์ .env (ถาวร)

สร้างไฟล์ `.env` ในโฟลเดอร์โปรเจค:

```bash
OPENROUTER_API_KEY=api-key-ของคุณ
OPENROUTER_MODEL=qwen/qwen3-vl-235b-a22b-instruct
```

จากนั้นรัน:
```bash
./start.sh
```

### วิธีที่ 3: LM Studio (ออฟไลน์ ไม่ต้องใช้ API)

1. ดาวน์โหลด [LM Studio](https://lmstudio.ai/)
2. โหลดโมเดลวิชั่น (เช่น LLaVA, Qwen-VL)
3. เปิดเซิร์ฟเวอร์ท้องถิ่น (พอร์ต 1234)
4. ในการตั้งค่าเว็บ เลือก "LM Studio"

## 📖 วิธีใช้งาน

### หน้าเว็บ

1. ลากและวาง PDF หรือ ZIP ลงในช่อง
2. การประมวลผลจะเริ่มอัตโนมัติ
3. ดาวน์โหลด ZIP ที่มีไฟล์ที่เปลี่ยนชื่อแล้ว

### บรรทัดคำสั่ง (CLI)

```bash
# ประมวลผลไฟล์เดียว
npm run dev process ใบแจ้งหนี้.pdf

# ประมวลผลโฟลเดอร์
npm run dev process ./โฟลเดอร์-ใบแจ้งหนี้/

# ประมวลผล ZIP
npm run dev process ใบแจ้งหนี้.zip

# พร้อมตัวเลือก
npm run dev process ใบแจ้งหนี้.zip --provider openrouter --dry-run
```

## 🎯 โมเดลที่แนะนำ

โมเดลที่ดีที่สุดสำหรับ OCR ใบแจ้งหนี้ภาษาไทย:

| โมเดล | คุณภาพ | ความเร็ว | ราคา |
|-------|--------|---------|------|
| **Qwen3-VL-235B** | ⭐⭐⭐⭐⭐ | ⭐⭐ | $$ |
| **Qwen3-VL-30B** | ⭐⭐⭐⭐ | ⭐⭐⭐⭐ | $ |
| **Qwen2.5-VL-72B** | ⭐⭐⭐⭐ | ⭐⭐⭐ | $ |
| **Gemini 2.5 Flash** | ⭐⭐⭐⭐ | ⭐⭐⭐⭐⭐ | $$ |
| **Claude Sonnet 4.5** | ⭐⭐⭐⭐⭐ | ⭐⭐⭐ | $$$ |

## 🔧 แก้ปัญหา

### เซิร์ฟเวอร์เปิดไม่ได้

```bash
# ลบและติดตั้งใหม่
rm -rf node_modules dist
npm install
npm run build
./start.sh
```

### ข้อผิดพลาด "OPENROUTER_API_KEY not found"

ตรวจสอบ API key:
```bash
echo $OPENROUTER_API_KEY
```

ถ้าว่างเปล่า ให้ export:
```bash
export OPENROUTER_API_KEY='key-ของคุณ'
```

### พอร์ต 3000 ถูกใช้งานอยู่แล้ว

หยุดโปรเซสที่มีอยู่:
```bash
lsof -ti:3000 | xargs kill -9
```

หรือเปลี่ยนพอร์ตใน `src/server/index.ts` (บรรทัด 15):
```typescript
const PORT = 3001; // แทนที่ 3000
```

## 📁 โครงสร้างไฟล์ที่เปลี่ยนชื่อแล้ว

รูปแบบ: `YYYY-MM-DD-ชื่อร้าน.pdf`

ตัวอย่าง:
- `2025-11-15-7-Eleven.pdf`
- `2024-03-20-Makro.pdf`
- `2025-01-05-Lotus.pdf`

## 🛑 หยุดเซิร์ฟเวอร์

กด `Ctrl+C` ในเทอร์มินัล

## 💡 เคล็ดลับ

- **ประมวลผลเป็นชุด**: วาง ZIP ที่มีหลาย PDF
- **หลายหน้า**: PDF หลายหน้าจะถูกแยกอัตโนมัติ
- **ปีพุทธศักราช**: วันที่ไทย (พ.ศ.) จะถูกแปลงอัตโนมัติ
- **ลองใหม่**: ไฟล์ที่ล้มเหลวสามารถลองใหม่ได้ทีละไฟล์

## 📞 ช่วยเหลือ

- เอกสารฉบับเต็ม: ดู README.md หลัก
- ปัญหา: [GitHub Issues](https://github.com/tarpediem/invoice-renamer-thai/issues)
- ตัวอย่าง: โฟลเดอร์ `examples/`

---

**ขอให้ประมวลผลใบแจ้งหนี้สนุก! 🎉**
